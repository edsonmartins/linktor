// emoji-mart v5 core ships no bundled TypeScript types for the vanilla Picker.
// We mount it imperatively (see components/emoji-picker.tsx), so `any` is fine.
declare module 'emoji-mart'
declare module '@emoji-mart/data'
